{
    aiscalator {
        since_version = 0.1.0
        created_on = generation_date

        timezone = Europe/Paris

        jupyter {
            dockerfile_src = ~/.aiscalator/docker
        }

        user {
            id = testUserID
        }
    }
}
